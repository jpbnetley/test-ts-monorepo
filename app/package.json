{
  "name": "app",
  "version": "1.0.0",
  "main": "dist/index.mjs",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "clean:build": "rimraf dist",
    "prebuild": "npm run clean:build",
    "build:types":"tsc --declaration --emitDeclarationOnly",
    "build": "npm run build:types && swc build src -d dist --strip-leading-paths --out-file-extension mjs",
    "watch": "tsc -w",
    "predev": "npm run build",
    "dev": "node dist/index.mjs",
    "start": "node dist/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@lib/web": "^1.0.0",
    "@lib/common": "^1.0.0",
    "@lib/node": "^1.0.0",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@swc/cli": "^0.4.0",
    "@swc/core": "^1.7.9",
    "rimraf": "^6.0.1"
  }
}
